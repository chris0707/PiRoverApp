<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chris.piroverapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mainicon2"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/mainicon2"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name="pirover.pinivea.chris.piroverapp.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pirover.pinivea.chris.piroverapp.NavigationDrawer" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name="pirover.pinivea.chris.piroverapp.LoginActivity" />
        <activity android:name="pirover.pinivea.chris.piroverapp.RegisterActivity" />
        <activity android:name="pirover.pinivea.chris.piroverapp.ManualActivity"/>
        <activity android:name="pirover.pinivea.chris.piroverapp.AutoActivity" />
    </application>

</manifest>