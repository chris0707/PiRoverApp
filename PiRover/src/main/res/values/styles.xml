<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/White</item>
        <item name="android:textColorHint">@color/White</item>
        <item name="android:textColorPrimary">@color/White</item>
        <item name="android:popupMenuStyle">@style/CustomOverflowBack</item>

    </style>

    <style name="CustomOverflowBack1" parent="android:style/Widget.Holo.Light.ListPopupWindow">
        <item name="android:popupBackground">@android:color/background_dark</item>
    </style>

    <style name="CustomOverflowBack" parent="Widget.AppCompat.Light.ListPopupWindow">
        <item name="android:popupBackground">@android:color/background_dark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" >
        <item name="android:background">@color/BrownShade</item>
    </style>



</resources>
